module Base.Map
  ( Map,
    fromList,
    insert,
    Map.lookup,
    mapWithKey,
    member,
    restrictKeys,
    toList,
  )
where

import Data.Map (Map, fromList, insert, mapWithKey, member, restrictKeys, toList)
import qualified Data.Map as Map (lookup)
